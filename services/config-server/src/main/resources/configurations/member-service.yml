
server:
  port: 8050
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/member
    username: arjay
    password: arjay
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
    user: ${spring.datasource.username}
    password: ${spring.datasource.password}

keyStore:
  path: keystore.jks
  password: password
#  security:
#    user:
#      name: arjay
#      password: user1234


application:
  config:
    organization-url: http://localhost:8222/api/v1/organization